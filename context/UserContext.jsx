import { createContext } from "react";

// since we need  to store some data like token and username or userid somewhere inside the code so
// we gonna store that data inside the context.
export const UserContext = createContext();